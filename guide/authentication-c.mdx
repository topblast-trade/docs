---
title: "用户接口鉴权"
---

用户接口使用用户访问 Token 识别已登录用户。通过标准 Bearer Authentication 在请求 Header 中传递 Token：

```http
Authorization: Bearer <access_token>
```

Token 由用户登录流程取得。客户端不得将 Token 放入查询参数或请求体。

<Note>
当前基础架构将 Bearer Token 格式标记为 JWT。实际 Token 的签发、刷新、过期时间和撤销规则需要在认证服务协议确定后补充。
</Note>
