---
title: "创建划转"
openapi: "/openapi/b/openapi.json POST /api/v1/broker/transfer"
---

<Note>
划转可能异步完成。业务码 `20201` 表示订单仍在 `PROCESSING`，不是失败。请等待 [`broker.transferred`](/cn/broker/api-reference/webhooks/broker-transferred) 事件，或使用相同 `txnId` 查询详情。不要生成新的 `txnId` 盲目重试。
</Note>
