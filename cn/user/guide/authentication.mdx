---
title: "API 鉴权"
---

Trading REST API 使用用户访问 Token 识别已登录用户。通过标准 Bearer Authentication 在请求 Header 中传递 Token：

```http
Authorization: Bearer <access_token>
```

Token 由用户登录流程取得。客户端不得将 Token 放入查询参数或请求体。

